# MailOps frontend environment
# Copy to .env.local

# Base URL of the MailOps API. The browser talks to it directly (the API sets
# httpOnly cookies, so requests must carry credentials).
NEXT_PUBLIC_API_URL=http://localhost:4000
